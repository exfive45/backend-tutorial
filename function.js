// Modules
//commonJS, every file  is module (by default)
//Modules - Encapsulated Code (only share minimum)
const john = 'john'
const kouekam = 'peter'

const sayHi = (name) =>{
    console.log(`bonjour comment allez vous ${name}`)
}

sayHi('daniel')
sayHi('blaise')
sayHi(kouekam)